# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# Increased memory for faster builds.
org.gradle.jvmargs=-Xmx4096m -XX:+UseParallelGC -Dfile.encoding=UTF-8

# Enables parallel execution of tasks for faster builds in multi-module projects.
org.gradle.parallel=true

# Enables build caching to reuse outputs from previous builds.
org.gradle.caching=true

# Enables configuration on demand for faster configuration phase.
org.gradle.configureondemand=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables AndroidX and Jetifier (if needed, though modern apps should use AndroidX)
android.useAndroidX=true
android.nonTransitiveRClass=true
